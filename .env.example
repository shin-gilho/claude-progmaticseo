# Database (Vercel Postgres or local)
DATABASE_URL="postgresql://user:password@localhost:5432/programmatic_seo?schema=public"

# Encryption secret key (32+ characters, random string)
# Generate with: openssl rand -base64 32
ENCRYPTION_SECRET_KEY="your-very-secure-32-character-secret-key-here"

# Optional: Default AI API Keys (can also be stored encrypted in DB)
# CLAUDE_API_KEY="sk-ant-..."
# GEMINI_API_KEY="..."

# Optional: WordPress credentials (can also be stored encrypted in DB)
# WP_SITE_URL="https://your-wordpress-site.com"
# WP_USERNAME="your-username"
# WP_PASSWORD="your-application-password"
